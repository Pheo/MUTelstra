Django M2M server to be loaded on the VM

1. Dependencies:
a. Extract and do "python setup.py install" for each archive in Dependencies
b. nginx config file in Dependencies to be placed in /etc/nginx/sites-available

2. Usage
At command line:
$ sudo python ./manage.py runfcgi host=127.0.0.1 port=8080
To restart server (make sure do it in same dir)
$ sh restart.sh
